# Auditoría inicial — 2026-08-09

## Resultado

El sistema funcional actual está en `C:\laragon\www\papachito_movil`; el legado está en `C:\laragon\www\wilcatsystems_papachito`. La PWA ya contiene venta, pagos, cola offline, sync, historial, anulación, QR y reportes según handoff; la ruta de persistencia real sigue siendo MySQL/Laravel.

## Verificaciones

- Documentación y `postgresql-migration/` leídas completamente.
- `package.json`: Next/Vinext, no Expo.
- `db/schema.ts`: vacío; no es la base operativa.
- `src/bootstrap.php`: DSN `mysql:` y puente `PuntoVentaController`.
- PostgreSQL/`psql`: no disponibles en el entorno auditado.
- No se modificaron bases ni se movieron carpetas del legado.

## Reutilizar

Diseño de `styles.css`/`report.css`, contratos HTTP, reglas de estados, nombres de productos históricos, QR, pruebas de UUID y flujo offline.

## Reemplazar progresivamente

Monolito `app.js`, acceso PDO directo mezclado con HTTP, puente Laravel para registrar ventas, IndexedDB como persistencia móvil cuando Expo/SQLite esté listo.

## Pendiente de aprobación

La siguiente acción estructural debe ser crear el adaptador `mysql|pgsql` y una prueba de lectura contra una base PostgreSQL nueva. No se debe generar APK ni desactivar MySQL antes de pruebas de equivalencia.
