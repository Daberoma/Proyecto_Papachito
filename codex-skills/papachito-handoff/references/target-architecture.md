# Arquitectura objetivo

`mobile/` Expo + SQLite → `backend/` API y servicios → `database/` PostgreSQL. `C:\laragon\www\papachito_movil` y Laravel se mantienen durante la transición hasta pasar equivalencia. Consultar `docs/FILE-MAP.md` y `docs/FLOW-MAP.md` antes de explorar.
